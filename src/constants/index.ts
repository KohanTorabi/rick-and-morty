export const FAVORITE_LIST_STORAGE_KEY = "favorites";

export enum RoutesPath {
  Home = "/",
  Favorites = "/favorites",
}

export const URLs = {
  allCharacters: `https://rickandmortyapi.com/api/character`,
};
